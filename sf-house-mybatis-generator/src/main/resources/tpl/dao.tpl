package ${daoPackage};

import ${baseMapper};
import ${domainPackage}.${className};

/**
 * Created by ${author} on ${currentDate}.
 */

public interface ${className}${daoType} extends BaseMapper<${className}> {


}